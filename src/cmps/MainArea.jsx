import { Outlet } from 'react-router-dom';

import Header from "./Header"
export default function MainArea(){

    
    
    return(
        <section>
            <Header/>
            < Outlet/>
        </section>
    )}
    
